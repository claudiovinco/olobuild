# Audit standard tile — esito finale migrazione (v1.4.149)

Migrazione completata 2026-06-10: **51 findings migrati** (renderer dual-format: i dati legacy rendono identici), **46 chiusi senza modifica** con le motivazioni sotto (falsi positivi, pattern già a standard, feature non coppie base/hover, conversioni che perderebbero funzionalità).

## Migrati (51)

- **announcementbar** `letter_spacing` — Ricetta D: config src/config/elements/announcementbar.js da { type: 'text' } a { type: 'unit', units: ['em','px','rem'], min: 0, step: 0.05 } (label invariata). Il formato salvato resta la stessa stri
- **asciiviz** `radius` (renderer dual-format) — Ricetta A: config src/config/elements/asciiviz.js da range 0-40 a { type: 'border-radius' } (stessa key, label invariata, default 18 numerico mantenuto — FieldBox lo gestisce in linked mode). PHP incl
- **beforeafter** `radius` (renderer dual-format) — Ricetta A: config src/config/elements/beforeafter.js da range 0-32 a { type: 'border-radius' } (stessa key, label 'Raggio bordo (px)' invariata, default 12 numerico mantenuto). PHP includes/tiles/clas
- **D:\TECNICA\olobuild\src\config\elements\countdown** `item_radius` — Ricetta A: field 'Raggio angoli card (px)' convertito da type 'range' (0-40) a type 'border-radius' — stessa key, stessa label. Default '0' scalare lasciato invariato (FieldBox gestisce il legacy scal
- **D:\TECNICA\olobuild\includes\tiles\class-countdown-tile** `item_radius` (renderer dual-format) — Renderer PHP dual-format in ENTRAMBI i rami (render_custom righe ~111/131 e render_uikit ~310/329): absint($s['item_radius']) sostituito con $this->build_border_radius_css($s['item_radius'] ?? 0); l'e
- **D:\TECNICA\olobuild\src\components\Tiles\CounterTile** `border_radius (+ tile_padding)` (renderer dual-format) — Fix WYSIWYG solo-Vue (formato dati INVARIATO, config e PHP non toccati): wrapStyle ora usa toRadiusCss(s.border_radius, 0) object-aware (prima parseInt(oggetto)=NaN→0px) e toSpacingCss(s.tile_padding 
- **finder** `chip_radius` (renderer dual-format) — Config (src/config/elements/finder.js): range 0-999 -> type 'border-radius' (stessa key, label invariata, default '999' scalare gestito da FieldBox). PHP (includes/tiles/class-finder-tile.php:104): in
- **form** `submit_hover_bg` — Config (src/config/elements/form.js): rimosso il field separato 'Sfondo pulsante hover' e wrappato il base in withHover({ key: 'submit_bg', label: t('Sfondo pulsante'), type: 'color' }, { hoverKey: 's
- **gallery** `img_height` — Config (src/config/elements/gallery.js:95): type 'text' -> { type: 'unit', units: ['px'], min: 0, step: 10 }. Formato salvato invariato (stringa CSS '200px'); units solo px perche' class-gallery-tile.
- **headline** `text_shadow` — Config src/config/elements/headline.js: le option 'Bagliore'/'Bagliore forte' ora usano '0 0 10px/20px var(--olo-color-primary)' (token brand); le value legacy indaco 'rgba(99,102,241,…)' restano come
- **hiddenpop** `key_sequence_confetti_colors` (renderer dual-format) — Config src/config/elements/hiddenpop.js: il text CSV 'Colori coriandoli' sostituito da 3 color picker confetti_color_1..3 (pattern goo.js color_1..color_5, default '' = palette brand), condizionati a 
- **info-cards** `(file)` — sections-reorg: inseriti 4 separator negli itemFields di 'items' in src/config/elements/info-cards.js (Media prima di media_image, Counter prima di counter, Testi prima di title, Footer & Link prima d
- **instagram** `width` — ricetta D: type 'text' → { type: 'unit', units: ['px','%'], min: 0 } in src/config/elements/instagram.js (label e placeholder invariati). Formato salvato invariato (stessa stringa CSS, default '100%')
- **introsplit** `media_radius` (renderer dual-format) — ricetta A dual-format: config range → type 'border-radius' (stessa key/label, default numerico 20 invariato). PHP class-introsplit-tile.php:72-74: intval sostituito con build_border_radius_css(?: '0px
- **leaderboard** `bar_radius` (renderer dual-format) — ricetta A dual-format: config range → type 'border-radius' (stessa key/label, default 6 invariato). PHP class-leaderboard-tile.php:126: max/min/intval sostituito con build_border_radius_css($s['bar_ra
- **linkinbio** `items.icon` (renderer dual-format) — ricetta G (parity Vue/PHP, formato dati invariato): class-linkinbio-tile.php ora rende item.icon dentro il bottone link via render_icon_html (stesso helper già usato per l'avatar placeholder) con fall
- **navmenu** `v_hover_bg` — Ricetta B in src/config/elements/navmenu.js: rimosso il field hover separato { key: 'v_hover_bg', type: 'color' } e wrappato il base in withHover({ key: 'v_active_bg', label: t('Sfondo attivo'), type:
- **mobilebar** `panel_item_padding` (renderer dual-format) — Fix del mismatch tipo field ↔ renderer (opzione preferita del proposal): renderer resi DUAL-FORMAT, config invariato (già type 'spacing'; FieldSpacing gestisce nativamente il default scalare '16' in l
- **newsletter** `btn_hover_bg` — Rimosso il field separato 'Sfondo hover' (color); il base btn_bg è ora withHover({ key:'btn_bg', type:'color' }, { hoverKey:'btn_hover_bg' }). Chiavi salvate invariate; renderer PHP non toccato (class
- **newsletter** `btn_color_hover` — Rimosso il field separato 'Colore testo hover'; aggiunto colorHover:'btn_color_hover' alle keys del gruppo typography 'Pulsante' (che già gestiva color:'btn_color'). FieldTypography.vue:306 supporta k
- **class-overlaygrid-tile** `item_radius` (renderer dual-format) — Config: range → type 'border-radius' (stessa key/label, default 12 numerico, FieldBox gestisce lo scalare legacy). PHP dual-format: max(0,intval(...)) → $this->build_border_radius_css($s['item_radius'
- **class-overlayslider-tile** `slide_radius` (renderer dual-format) — Config: range → type 'border-radius' (stessa key/label, default 0 numerico). PHP dual-format: max(0,intval(...)) → $this->build_border_radius_css($s['slide_radius'] ?? 0) e 'border-radius: {css};' — o
- **olo_room_grid** `card_border_hover + card_hover_border_width` — Conversione parziale della famiglia hover: card_border_color ora withHover(..., { hoverKey:'card_border_hover' }) e card_border_width (range) ora withHover(..., { hoverKey:'card_hover_border_width' })
- **olo_room_availability** `title` — Select di 4 frasi fisse → type 'text' libero (stessa key, stesso formato stringa; le 4 frasi salvate restano valide; '' = nessun titolo, segnalato in description). Verifica renderer esterno eseguita c
- **pagination** `button_padding` (renderer dual-format) — type 'text' ('V H') -> type 'spacing' max 50 (stessa chiave, default '8 16' invariato). PHP class-pagination-tile.php: parsing dual-format (is_array -> Olo_Tile_Utils::spacing_css, altrimenti percorso
- **pagination** `active_color` (renderer dual-format) — Field morto rimosso: campo 'Colore testo pagina attiva' eliminato da styleFields e dai defaults (config + PaginationTile.vue + class-pagination-tile.php), inclusa l'assegnazione PHP $active_color mai 
- **PaginationTile** `border_radius` (renderer dual-format) — Parity-fix WYSIWYG (config gia' a standard withHover+border-radius, non toccato): borderRadius ora usa radiusToCss(s.value.border_radius, {fallback:'4px'}) da '@/composables/useRadius' al posto di par
- **panelslider** `card_image_radius` (renderer dual-format) — type 'range' (0-40) -> type 'border-radius' (stessa chiave, label e description '0 = eredita dal raggio card' invariate, default numerico 0 lasciato). PHP class-panelslider-tile.php:328/336: intval ->
- **popup** `button_radius` (renderer dual-format) — type 'range' (0-999) -> type 'border-radius' (stessa chiave, default 6 invariato). PHP class-popup-tile.php:261/379: intval -> $this->build_border_radius_css($s['button_radius'] ?? 6) con vuoto -> '0p
- **postmeta** `chip_radius` (renderer dual-format) — type 'range' (0-999) -> type 'border-radius' (stessa chiave, condition chip_style!=none invariata, default 0). PHP class-postmeta-tile.php:121/302: absint+clamp -> $this->build_border_radius_css(); bo
- **paymentbuttons** `amount` — type 'text' -> type 'number' min 0 step 0.01. Il field number nativo emette la stessa stringa raw ('29.99', '' se vuoto): formato salvato invariato, renderer non toccati (PHP esc_attr + data-amount, V
- **productgrid** `card_radius` (renderer dual-format) — Ricetta A (radius-as-range): config src/config/elements/productgrid.js type 'range'->'border-radius' (key/label/default 0 invariati). PHP class-productgrid-tile.php: intval -> $this->build_border_radi
- **scrollscrub** `round` (renderer dual-format) — Ricetta A (radius-as-range): config src/config/elements/scrollscrub.js type 'range'->'border-radius' (key/label/default 14 invariati). PHP class-scrollscrub-tile.php:135 max(0,min(60,intval)) -> $this
- **progallery** `img_height` — Ricetta D (text con unita CSS): config src/config/elements/progallery.js type 'text' -> { type: 'unit', units: ['px','vh'], min: 0 }, label e condizione show invariate. FieldUnit salva la STESSA strin
- **stackscroll** `round` (renderer dual-format) — Ricetta A: config src/config/elements/stackscroll.js — field 'round' da type 'range' (min 0, max 40) a type 'border-radius', stessa key e label invariata, default 20 numerico lasciato (FieldBox gestis
- **svganimator** `max_width` (renderer dual-format) — Ricetta D: config src/config/elements/svganimator.js — da type 'text' a { type: 'unit', units: ['px','%'], min: 0 }, label aggiornata in t('Larghezza max (vuoto = 100%)'). Formato salvato invariato (s
- **shatteredimage** `height` — Ricetta D (text con unità CSS, nota unità nel finding): config src/config/elements/shatteredimage.js — da type 'text' a { type: 'unit', units: ['px','vh'], min: 0 }. Default '400px' invariato. Formato
- **switcher** `tab_radius` (renderer dual-format) — Ricetta A (radius-as-range): config src/config/elements/switcher.js → type 'border-radius' (stessa key/label, default '8' invariato); PHP class-switcher-tile.php → build_border_radius_css($s['tab_radi
- **switcher** `container_radius` (renderer dual-format) — Ricetta A: config → type 'border-radius' (default '10' invariato); PHP → $cont_rad_css = build_border_radius_css(...), emissione condizionale identica al legacy (0/vuoto → nessuna regola border-radius
- **switcherpanel** `hero_radius` (renderer dual-format) — Ricetta A su 3 lati: config src/config/elements/switcherpanel.js → type 'border-radius'; PHP class-switcherpanel-tile.php → $hero_rad_css = build_border_radius_css(...) con emissione condizionale (0 →
- **switcherpanel** `nav_radius` (renderer dual-format) — Ricetta A: config → type 'border-radius'; PHP → build_border_radius_css(...) ?: '0px' (emissione incondizionata, 0 → '0px' identico al legacy); Vue → radiusToCss fallback '0px' in navBtnStyle.
- **switcherpanel** `nav_container_radius` (renderer dual-format) — Ricetta A: config → type 'border-radius'; PHP → $nav_cont_rad_css condizionale (0 → nessuna regola, identico); Vue → radiusToCss fallback '0px' in navStyle.
- **switcherpanel** `panel_radius` (renderer dual-format) — Ricetta A: config → type 'border-radius'; PHP → $panel_rad_css condizionale (identico per 0); Vue → radiusToCss fallback '0px' in panelStyle.
- **switcherpanel** `panel_image_radius` (renderer dual-format) — Ricetta A: config → type 'border-radius'; PHP → $panel_img_rad_css usato nei 2 punti CSS (media + img), 'overflow:hidden' resta legato a radius non-zero come prima; Vue → radiusToCss fallback '0px' in
- **timeline** `tl_card_radius` (renderer dual-format) — Ricetta A con semantica '0 = auto variante' preservata: config src/config/elements/timeline.js → type 'border-radius' (label invariata); PHP class-timeline-tile.php → nuovo closure $rad in custom_styl
- **timeline** `tl_media_radius` (renderer dual-format) — Ricetta A: stessa migrazione di tl_card_radius (config border-radius, PHP $rad('tl_media_radius','--tl-media-radius'), Vue rad('tl_media_radius','--tl-media-radius')); semantica 0=auto invariata.
- **togglebtn** `btn_hover_bg` — Ricetta B (legacy-hover-field): rimosso il field separato { key: 'btn_hover_bg', type: 'color' } da src/config/elements/togglebtn.js e wrappato il base in withHover({ key: 'btn_bg', label: 'Sfondo', t
- **D:\TECNICA\olobuild\src\config\elements\woo_addtocart** `hover_bg` — Ricetta B: rimosso il field separato { key: 'hover_bg', type: 'color' } e wrappato il field base in withHover({ key: 'bg_color', label: t('Sfondo'), type: 'color' }, { hoverKey: 'hover_bg' }). Base e 
- **D:\TECNICA\olobuild\src\config\elements\woo_addtocart** `hover_text` — Ricetta B: rimosso il field separato { key: 'hover_text', type: 'color' } e wrappato il field base in withHover({ key: 'text_color', label: t('Colore testo'), type: 'color' }, { hoverKey: 'hover_text'
- **D:\TECNICA\olobuild\src\config\elements\woo_product_navigation** `hover_color` — Ricetta B (variante typography-block, come da proposal: il colore base vive nel blocco typography): rimossi il separator 'Colori' e il field separato { key: 'hover_color', type: 'color' }; aggiunto co
- **D:\TECNICA\olobuild\src\config\elements\woo_product_title** `link_color_hover` — Ricetta B (variante typography-block, come da proposal: il colore base 'color' vive nel blocco typography): rimossi il separator 'Colori' e il field separato { key: 'link_color_hover', type: 'color' }

## Chiusi senza modifica (46)

- **accordion** `hover_image` — Ricetta B non applicabile: è un itemField dentro content-items 'panels' (accordion.js:83) — swap di CONTENUTO per-item (immagine mostrata al passaggio), non variante hover di un field di stile. Non esiste base field di stile corrispondente e ContentI
- **alert** `content-popup (Testi)` — Falso positivo dell'audit, nessuna modifica al codice: 'content-popup' è un type reale e funzionante implementato in InspectorField.vue (righe 68 e 267, verificate). L'azione corretta è aggiungerlo alla whitelist dei tipi legali dell'auditor, non toc
- **button** `hover_image, hover_video` — Ricetta B non applicabile: sono feature standalone (media alternativi mostrati al passaggio del mouse), non coppie base+hover di una property CSS — hover_video non ha alcun field base e il config documenta esplicitamente l'eccezione (button.js:165-16
- **categoryrail** `radius` — Il controllo a 4 angoli esiste GIÀ: 'card_radius' (type border-radius, default {14,14,14,14}) è l'override gated che sovrascrive il base via build_border_radius_css (class-categoryrail-tile.php:95-107). Convertire anche 'radius' (range legacy, defaul
- **content** `hover_image / hover_video / hover_effect` — Ricetta B non applicabile: non sono coppie base/hover di stile ma una feature di swap CONTENUTO media al passaggio del mouse (hover_image type image, hover_video type media — nessun field base dello stesso type su cui agganciare withHover; hover_vide
- **featuredstory** `media_radius` — Il per-angolo esiste GIÀ: 'cover_radius' (type border-radius, featuredstory.js:141-142) con fallback DOCUMENTATO e by-design su media_radius quando tutti gli angoli sono 0 (class-featuredstory-tile.php:116-119: build_border_radius_css(cover_radius) ?
- **code** `language` — Ricetta F richiede options ESATTE dai valori accettati dal renderer, ma class-code-tile.php NON ha alcuna whitelist di linguaggi (verificato: righe 61-62 $lang_class = "language-{$language}" su stringa arbitraria, riga 97 badge esc_html as-is; l'unic
- **floatingpanel** `trigger_radius` — Semantica PERCENTO, non px: il PHP emette 'border-radius:{$t_radius}%' (class-floatingpanel-tile.php, riga del button trigger; 50 = cerchio perfetto). Il type 'border-radius' salva px per-angolo: la conversione cambierebbe la semantica del controllo 
- **glowgallery** `strip_radius` — Non e' un border-radius semplice: il PHP lo usa asimmetricamente come '{$srad}px {$srad}px 8px 8px' (solo angoli superiori, bottom fisso 8px = forma-firma ad arco della tile, class-glowgallery-tile.php:119). Il controllo per-angolo esiste gia' via 'm
- **goo** `(file)` — Categoria 'other' con proposal non circoscritto (scelta a/b) e premessa parzialmente errata: verificato che in class-goo-tile.php $pad_y/$max_w/$align_v/$align_h/$text_al/$full/$base/$shadow_css/$content_html vengono calcolati (righe 135-148) ma MAI 
- **grid** `items.hover_image / items.hover_video` — Verificato (grid.js:66-67,75 + class-grid-tile.php render_hover_wrap): sono itemFields di CONTENUTO dentro content-items (media alternativi mostrati al passaggio), non coppie base/hover di stile; ContentItemsEditor.vue non supporta withHover (0 occor
- **hoverlist** `hover_indent / hover_bg` — Verificato (hoverlist.js:35-36,95-96): l'hover È la feature della tile (riga che si indenta e colora al passaggio); non esiste alcun field base corrispondente (nessun row_bg/indent base) su cui agganciare withHover. Convertire richiederebbe una chiav
- **image** `hover_image` — Verificato (image.js:95-97 sezione 'Sostituzione al passaggio del mouse', ImageTile.vue e class-image-tile.php:render_hover_wrap): feature di CONTENUTO (swap media su hover), non variante hover di un field di stile; hover_video non ha alcun field bas
- **imgcompare** `card_border_width` — Verificato (imgcompare.js:33-34,101-102 + class-imgcompare-tile.php:69-70 intval/safe_color_css sui due scalari): la conversione a { key:'card_border', type:'border' } cambierebbe il formato dati (2 scalari → oggetto border) e richiederebbe build_bor
- **linkinbio** `background_gradient` — Cambio formato dati: FieldGradient (type 'gradient') salva un oggetto {type, angle, stops} mentre entrambi i renderer (class-linkinbio-tile.php:86-91 preg_replace su stringa CSS usata diretta come background; LinkInBioTile.vue:104 stringa diretta) si
- **loginform** `border_width` — Migrare il bordo del form al type 'border' standard cambia il formato dati salvato (due scalari border_width range + border_color color → oggetto border) e richiede rework del renderer PHP (class-loginform-tile.php:118 intval($s['border_width']) + co
- **lottie** `json_url` — Report-only senza azione sul codice: 'lottie_picker' è un tipo custom reale e funzionante, registrato in src/components/Builder/InspectorField.vue:156 (verificato dall'evidence). L'unica azione proposta è aggiungere il tipo alla whitelist dei tipi le
- **map** `address` — Report-only senza azione sul codice: 'geocode' è un tipo custom reale (ricerca Nominatim che scrive su targetLat/targetLng/targetZoom), implementato in InspectorField.vue:317-338 + geocodeAddress. Da aggiungere alla whitelist dell'auditor, nessuna co
- **megamenu** `panel_templates` — Report-only senza azione sul codice: 'megapanel-map' è un tipo custom reale (FieldMegaPanelMap, registrato in InspectorField.vue:180 e :681). Da aggiungere alla whitelist dell'auditor, nessuna conversione.
- **map** `latitude / longitude` — Verificato nel codice: i due field sono type 'text' e DYNAMIC_TYPES in InspectorField.vue:452 è ['text','textarea','editor','image','media'] → convertire a type 'number' farebbe perdere il dynamic binding ⚡ (stessa caveat della ricetta E per osmmap: 
- **matchfixtures** `radius` — Conforme al proposal del finding: il controllo standard a 4 angoli esiste già — 'card_radius' (type 'border-radius', reso via build_border_radius_css in class-matchfixtures-tile.php:80) è l'override documentato nel config ('Vuoto/0 = usa il Raggio (p
- **navmenu** `mobile_type` — Consolidamento UX non meccanico, non a formato-dati invariato (ricetta G): lo stato legacy mobile_style='dropdown'/'offcanvas' + mobile_type='fullscreen' (verificato: PHP usa mobile_type SOLO per il ramo fullscreen, righe 400-401/562-564, mentre offc
- **newsletter** `icon_name` — text-should-be-icon NON convertibile a formato-dati invariato: i renderer stampano la stringa LETTERALE (class-newsletter-tile.php:203 esc_html, NewsletterTile.vue:5 interpolazione) — un nome del set SVG di FieldIcon ('star') verrebbe reso come testo
- **osmmap** `address` — Finding report-only senza azione sul codice: type 'geocode' è un tipo custom reale e funzionante (InspectorField.vue:317-338 + geocodeAddress). Va aggiunto alla whitelist dei tipi legali dell'auditor, nessuna modifica al config.
- **osmmap** `latitude` — Conversione a type 'number' perderebbe il dynamic binding ⚡: DYNAMIC_TYPES = ['text','textarea','editor','image','media'] (InspectorField.vue:452) non include 'number', e anche forzando allowDynamic:true il path dinamico usa la map fieldComponent (ri
- **osmmap** `longitude` — Stessa motivazione di latitude: 'number' non è in DYNAMIC_TYPES e manca dal fieldComponent map del path dinamico → si perderebbe il binding ⚡ senza nemmeno ottenere lo spinner. Skip per conservare il binding.
- **olo_room_grid** `card_hover_bg` — Il field base card_bg è condizionale (visibile solo con card_bg_type='color'): wrappandolo in withHover il controllo hover sparirebbe in modalità gradiente, mentre oggi card_hover_bg è sempre disponibile e il renderer lo applica a prescindere dal tip
- **olo_room_grid** `card_hover_shadow (+6 sub-field custom) / card_hover_lift` — Non sono coppie base/hover semplici: card_hover_shadow è una select con 6 sub-field condizionali (op 'custom') accoppiata alla select card_shadow con il proprio set di 6 sub-field — withHover non modella select+sub-field condizionali senza ristruttur
- **panelslider** `hover_shadow` — Ricetta B non applicabile: il field base e' il select 'shadow' del gruppo condiviso shadowField (_shared.js:48) le cui options includono 'custom' + 6 sub-field gated (shadow_h/v/blur/spread/color/inset); il select hover_shadow ha solo none/sm/md/lg/x
- **postgrid** `price_field` — Il proposal stesso e' esplicitamente report-only e segnala una regressione di flessibilita': convertire price_field/hover_image_field/hover_video_field/meta_key a select optionsSource 'metaKeys' impedirebbe di digitare meta-key protette/arbitrarie og
- **paymentbuttons** `icon_position` — Categoria 'other' con safety needs-renderer-check e proposal non circoscritto (due alternative): esporre il select richiederebbe di IMPLEMENTARE la resa dell'icona nel PHP frontend (class-paymentbuttons-tile.php non ha alcuna occorrenza di icon_posit
- **pricing** `cta_hover_text_color` — Ricetta B non applicabile: il field base cta_text_color non esiste come field standalone nel config - e gestito solo come keys.color dentro il blocco typography 'CTA' (pricing.js:197-202). withHover richiede un field base wrappabile dello stesso type
- **progallery** `hover_*` — Gli 11 field hover_ (hover_effect, hover_zoom_scale, hover_tilt_angle, hover_magnetic_strength, hover_glow_*, hover_caption*, hover_frame_inset) sono parametri della feature 'Effetti hover'/didascalia condizionati a hover_effect/hover_caption (progal
- **progress** `bars` — Ricetta G: il proposal (textarea pipe 'etichetta|valore' -> content-items con itemFields) cambia il formato dati salvato (stringa -> array di oggetti) e richiede normalizzazione backcompat in entrambi i renderer - non e una migrazione a formato-dati 
- **queryloop** `hover_effect` — Non e una coppia base+hover: e un SELETTORE di effetto hover della card (select di 8 effetti, queryloop.js:280), feature della tile. hover_bg e gia gestito a standard via withHover su bg_color con hoverKey (queryloop.js:333). Match solo nominale del 
- **showcasegrid** `radius` — Il proposal del finding stesso indica 'nessuna conversione necessaria': esiste già 'card_radius' type border-radius come override gated che usa build_border_radius_css (PHP:75-76) / oggetto 4 angoli (Vue:124-133); il range legacy 'radius' resta come 
- **sitelogo** `hover_opacity` — Ricetta B inapplicabile: non esiste un campo base 'opacity' di cui hover_opacity sarebbe la variante — è un effetto standalone consumato direttamente nel selettore :hover dal PHP (class-sitelogo-tile.php:104-106). withHover richiederebbe di introdurr
- **smearhero** `smear_palette` — Ricetta G: proposal non applicabile a formato-dati invariato con l'infrastruttura disponibile — è una lista CSV di colori ('#e0b13a,#cc5b3f,…') parsata da entrambi i renderer (PHP explode(','), Vue split(',')); servirebbe un nuovo field type 'color-l
- **switcher** `hover_bg` — Ricetta B non applicabile: non esiste un field base 'sfondo tab inattiva' su cui agganciare withHover — active_bg è lo stato ATTIVO (altro stato, non il base) e la regola PHP :hover agisce su :not(.uk-active) (class-switcher-tile.php:296-302). Wrappa
- **switcherpanel** `nav_hover_bg` — Stesso caso di switcher.hover_bg: nessun field base 'sfondo voce inattiva' esistente (nav_active_bg è lo stato attivo; l'hover PHP agisce su .olo-sp-nav__btn:not(.is-active), class-switcherpanel-tile.php). withHover richiederebbe un base nuovo = camb
- **tripfinder** `radius` — Pattern intenzionale già a standard: esiste l'override per-angolo 'radius_corners' (type border-radius) che usa build_border_radius_css con semantica 'tutti 0 = usa radius uniforme' (class-tripfinder-tile.php:69-71). Convertire anche 'radius' creereb
- **viewer360** `video_url` — Categoria 'other' con proposal esplicitamente report-only/decisione UX: FieldMedia non ha input manuale per URL esterni e il campo è pensato proprio per URL esterni (placeholder 'https://...video-360.mp4', viewer360.js:82). Convertire a type 'media' 
- **D:\TECNICA\olobuild\src\config\elements\woo_recently_viewed** `hover_effect` — Match solo letterale del prefisso hover_: è un select di effetto-su-hover della card (none/zoom/shadow, default 'zoom'), non la variante hover di un field base — non esiste un field base 'effect' da wrappare con withHover(). Il proposal stesso dice '
- **D:\TECNICA\olobuild\src\config\elements\workgrid** `hover_zoom` — Toggle comportamentale (abilita zoom immagine al hover), non variante hover di un field base: withHover() non si applica a un toggle di effetto e non esiste field base 'zoom'. Il proposal stesso dice 'Nessuna conversione'. Lasciato com'è, chiave pres
- **D:\TECNICA\olobuild\src\config\elements\worklist** `hover_indent` — Range parametrico dell'animazione hover (quanti px indentare la riga), non variante hover di un field base (non esiste 'indent' base; type range ≠ pattern color+color). Il proposal stesso dice 'Nessuna conversione'. Lasciato com'è, chiave preservata.
- **D:\TECNICA\olobuild\src\config\elements\worklist** `row_hover_bg` — Stile di stato hover senza field base corrispondente: non esiste 'row_bg' nel config, quindi withHover() non avrebbe nulla da wrappare. Finding di solo censimento ('Solo segnalazione… lasciare com'è'). Lasciato com'è, chiave preservata.
